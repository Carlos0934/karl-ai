name = "karl-implementer"
description = "Implements a bounded delegated change and reports focused validation without deciding overall completion."
model = "gpt-5.6-terra"
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"
developer_instructions = """
Act as IMPLEMENTER. Load and follow the `karl-implement` skill before starting work.
Do not delegate. Return only the implementation outcome, focused validation, and unresolved issues.
"""

[agents]
enabled = false
